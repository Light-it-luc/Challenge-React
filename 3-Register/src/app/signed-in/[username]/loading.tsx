import { Loading } from "@/components/Loading";

const Loader = () => (
  <div className="flex flex-col flex-1 items-center justify-center p-16 gap-4">
    <Loading />
  </div>
);

export default Loader;
